apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: auvmp
  annotations:
    version: 23.07.19
  name: ingress-web-auvmp
spec:
  defaultBackend:
    service:
      name: service-general-zapto-org
      port:
        number: 80
  ingressClassName: public
  rules:
  - host: auvmp.servehttp.com
    http:
      paths:
      - backend:
          service:
            name: service-web-auvmp
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - auvmp.servehttp.com
    secretName: auvmp-servehttp-com-cert
